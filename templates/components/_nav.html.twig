<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('index') }}">E-Shop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav me-auto">

            <a class="nav-link active"  href="{{ path('default_category',{'alias' : 'telephone'}) }}">Telephone</a>
            <a class="nav-link active"  href="{{ path('default_category',{'alias': 'ordinateur'}) }}">Ordinateur</a>
        </div>
        <div class="navbar-nav ms-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a class="nav-link active" href="{{ path('product_create') }}">Ajouter un produit</a>
                <a class="nav-link active" href="{{ path('app_logout') }}">Deconnexion</a>

            {% else %}
                <a class="nav-link active" href="{{ path('user_register') }}">Inscription</a>
                <a class="nav-link active" href="{{ path('app_login') }}">Connexion</a>
        {% endif %}
        </div>
    </div>
</nav>